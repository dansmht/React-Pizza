const initialState = {};

const cart = (state = initialState, { type }) => {
  switch (type) {
    case 'TEST_TEST':
      return state;
    default:
      return state;
  }
};

export default cart;
